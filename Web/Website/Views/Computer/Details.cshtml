@model Roomie.Web.Persistence.Models.Computer

@{
    ViewBag.Title = Model.Name;
    var viewModel = new ComputerViewModel(Model, Url)
        {
            DisplayWebhookSettings = true
        };
}

@Html.Partial("Partials/Computer/Computer", viewModel)