@model NetworkModel
@*

*@<div class="buttonGroup two">@*

*@@using (Ajax.BeginForm(
    actionName: "AddDevice",
    controllerName: "Network",
    routeValues: new
    {
        id = Model.Id,
        returnUrl = Request.Path
    },
    ajaxOptions: new AjaxOptions
    {
        HttpMethod = "Post",
    },
    htmlAttributes: new
    {
        @class = "button leftmost"
    }
    ))
    {<button class="button">Add Device</button>}@*

*@@using (Ajax.BeginForm(
    actionName: "RemoveDevice",
    controllerName: "Network",
    routeValues: new
    {
        id = Model.Id,
        returnUrl = Request.Path
    },
    ajaxOptions: new AjaxOptions
    {
        HttpMethod = "Post",
    },
    htmlAttributes: new
    {
        @class = "button rightmost"
    }
    ))
    {<button class="button">Remove Device</button>}@*

*@</div>@*

*@