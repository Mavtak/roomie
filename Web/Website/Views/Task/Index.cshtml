@model IEnumerable<Roomie.Web.Persistence.Models.Task>

@{
    ViewBag.Title = "Task Queue";
}

@if (Model == null || !Model.Any())
{
    @Html.Partial("Partials/BotMessage/Empty")
}
else
{
    foreach (var task in Model)
    {
        @Html.Partial("Partials/Task/Task", task);
    }
}